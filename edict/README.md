# edict 前端信息架构调整记录

## 本次任务目标

本次改造围绕用户提出的信息架构重组要求展开，目标是将原有的入口拆分方式重新收口到更清晰的任务看板、Agent 管理与控制台结构中，同时修复被错误改写的 **Agent** 称呼，并移除冗余或误导性的交互选项。

## 完成清单

| 状态 | 项目 | 说明 |
| --- | --- | --- |
| [x] | 技能管理助手命名修复 | 将“功能整理助手”统一收口为“技能管理助手”，并同步修复任务详情等关联展示文案。 |
| [x] | 任务看板整合入口 | 将任务模板能力并入任务看板发布语境，弱化原独立“提示词中心”表述。 |
| [x] | 回复风格并入 Agent 管理 | 取消独立“回复风格”入口，将相关模型切换能力并入 Agent 管理区域。 |
| [x] | 设置开关并入控制台 | 将设置面板收口到控制台区域，避免分散入口。 |
| [x] | 协作工作台命名调整 | 导航与登录页统一使用“Agent 协作工作台”。 |
| [x] | 快速发布冗余项移除 | 去掉“快速发布任务指定处理方式”相关显式选择，保持自动处理逻辑。 |
| [x] | 协助处理对象文案修复 | 将“协助处理的人”改为“协助处理的 Agent”。 |
| [x] | SOUL 正式名称对齐 | 依据各 Agent 的 `SOUL.md`，修正 Agent 调基与管理视图中的展示名称和职责摘要，避免继续沿用非正式别名。 |
| [x] | Agent 调基模块收纳 | 将 Agent 调基改为可折叠模块，并把方案切换、提醒方式与最近设置记录统一收纳在同一区域。 |
| [x] | 构建验证 | 前端已成功完成 `npm run build`，当前版本可以正常打包。 |

## 关键改动摘要

| 文件 | 主要调整 |
| --- | --- |
| `frontend/src/store.ts` | 收口导航页签定义，保留任务看板、Agent 协作工作台、控制台、Agent 管理、技能管理助手等结构；修复部分 Agent 元数据文案。 |
| `frontend/src/App.tsx` | 重组页面渲染分支，将设置面板合并进控制台，将模型配置合并进 Agent 管理，并保持任务看板为统一发布入口。 |
| `frontend/src/components/EdictBoard.tsx` | 重构任务看板发布区，去掉冗余处理方式措辞，统一任务模板入口说明，并修复技能管理助手相关文案。 |
| `frontend/src/components/SkillsConfig.tsx` | 将技能治理对话文案统一为“技能管理助手”，并调整摘要说明措辞。 |
| `frontend/src/components/TemplatePanel.tsx` | 将“提示词中心”文案改为“任务模板”，使其定位与任务看板发布流程一致。 |
| `frontend/src/components/OfficialPanel.tsx` | 对 Agent 管理页中的“成员/团队”类关键描述进行收口，恢复为 Agent 语义。 |
| `frontend/src/components/ModelConfig.tsx` | 将 Agent 调基改为可折叠容器，并统一通过正式角色元数据显示卡片标题、职责说明、提醒方式与最近设置记录。 |
| `frontend/src/components/TaskModal.tsx` | 修复 `admin_specialist` 的展示名称，避免旧称呼残留在任务详情中。 |

## 验证结果

本次修改后，已在 `frontend` 目录执行构建验证，结果为成功通过。新增核对了 Agent 调基模块的正式名称展示与折叠结构，构建未出现新的类型或语法错误。当前仍存在一个 **chunk 体积告警**，但这属于打包体积提醒，不影响本轮需求的功能正确性与构建完成。

| 验证项 | 结果 | 备注 |
| --- | --- | --- |
| TypeScript 构建 | 通过 | `tsc -b` 正常完成。 |
| Vite 打包 | 通过 | 产物已生成到 `dist/`。 |
| 体积告警 | 存在 | 主包超过 500 kB，后续可考虑按需拆包。 |

## 后续待办

| 状态 | 待办 | 说明 |
| --- | --- | --- |
| [ ] | 继续清理非核心页面中的旧别名 | 当前 Agent 管理与调基主路径已按 SOUL 收口，但其他辅助页面仍可继续做更彻底的正式名称统一。 |
| [ ] | 评估任务模板的进一步嵌入方式 | 如需彻底去除独立模板页面，可继续拆出轻量嵌入组件，仅在任务看板发布区展示。 |
| [ ] | 处理大体积 chunk 告警 | 可通过动态加载或手动分包优化首屏体积。 |

## 说明

本文件用于保留本次任务的项目痕迹，避免后续重复核对同一轮信息架构改造是否已经完成。
