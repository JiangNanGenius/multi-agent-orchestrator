# 统一 Todo（2026-04-08 收口合并版）

本文档用于替代仓库内此前并行存在的多份待办清单，作为当前项目唯一继续维护的主 Todo。此次合并基于 `todo.md`、`docs/current_todo_20260408.md`、README 最新预览区、最终收尾记录以及当前仓库状态复核结果完成，重点解决两类问题：其一，**多个 Todo 并行导致范围重复与状态冲突**；其二，**部分事项实际上已经完成，但历史清单没有及时打勾**。

## 一、当前结论

目前可以确认：看板认证改造、飞书群聊回复机制改造、公开界面第一轮脱敏、README 预览图补齐、前端构建产物去纳管、GitHub 推送、最终备份与项目留痕都已经完成。此前由于上下文恢复过程中又新增了一份 `docs/current_todo_20260408.md`，其中不少“待执行”事项在后续实际收口中已经完成，但没有回写，所以本次统一更正。

| 类别 | 当前状态 | 说明 |
|---|---|---|
| 主 Todo 入口 | 已统一 | 后续只维护本文件 |
| README 预览图补充 | 已完成 | 已加入任务发布、技能管理、全网搜索三张预览图 |
| 全网搜索页改造 | 已完成首轮闭环 | 已支持后台任务发起、历史保留、查看结果、查看任务、删除记录 |
| 前端构建产物治理 | 已完成 | `edict/frontend/dist/` 已改为不纳管 |
| 公开界面脱敏 | 已完成首轮收口 | 用户可见旧命名已基本替换，内部兼容 ID 保留 |
| GitHub 推送 | 已完成 | 最新已推送到当前远端 `main` |
| 备份与留痕 | 已完成 | 已生成 final 备份并写入最终工作日志 |
| 全仓深度重命名 | 已完成主线收口 | `agents.json`、`agents/groups/`、主调度脚本与测试范围已完成迁移；历史审计文档仍保留旧术语用于追溯 |

## 二、已完成并已补打勾的事项

下面这些事项在旧 Todo 中曾出现“未勾选”或“另一份 Todo 仍写待执行”的情况，但根据当前仓库事实，已经可以统一判定为完成。

### 2.1 本轮收口已完成事项

- [x] 盘点当前仓库未提交变更
- [x] 扫描遗漏的旧命名与未完成项目
- [x] 执行全仓脱敏检查
- [x] 补齐本轮必要修复
- [x] 完善 `README.md`，补入最新运行态预览图
- [x] 复核 Git 状态与变更边界
- [x] 提交并推送到 GitHub 当前远端 `main`
- [x] 形成最终收口说明并写入项目留痕
- [x] 生成最终完整备份包与校验文件
- [x] 将原 `zaochao` 页面改造成 `web_search` 全网搜索页
- [x] 完成全网搜索“发起后台任务 → 结果沉淀历史 → 支持回看与删除”闭环
- [x] 将前端构建产物目录加入忽略规则并从版本控制中移除
- [x] 清理公开界面中仍会直接暴露给用户的旧专家命名

### 2.2 历史主线已完成事项

- [x] 看板认证与访问控制改造完成闭环
- [x] 飞书群聊回复机制改造完成闭环
- [x] 项目文档与项目痕迹已补齐到仓库
- [x] 当前已改造内容完成构建与基础回归验证
- [x] README、README_EN、README_JA 的主入口口径已完成本轮同步
- [x] Docker 相关文件、目录与公开入口已移除
- [x] AI 优先部署口径已成为公开文档主路径

## 三、仍需继续推进的未完成事项

以下是合并两份 Todo 后，按当前事实保留下来的**真实未完成项**。后续应只围绕这些内容继续推进，不再重复保留已经收口的项目。

### 3.1 P0：必须继续收口的核心遗留项

- [x] 重构 `agents.json` 中的全部 agent ID、路径与调用链
- [x] 重命名 `agents/` 目录结构，并同步修改 SOUL 文档、职责关系与调用说明
- [x] 重构 `scripts/`、`edict/scripts/`、`migration/` 中残留的旧组织名、旧状态与旧 agent ID
- [x] 重构 `tests/` 中直接对外可见或会影响演示输出的旧 ID 与旧术语样例
- [x] 全仓二次扫描并确认旧 agent ID、旧术语与残余历史叙事已基本清零（源码/测试主链范围）

### 3.2 P1：文档与治理口径继续统一

- [x] 继续复核 README、安装文档、治理文档中是否仍存在“自动归档”类强表述，确保与真实实现一致
- [x] 输出一份更稳定的“当前真实能力边界”摘要，避免后续上下文恢复再次误判范围
- [x] 将归档、交付、自动化巡检之间的边界说明并入长期治理入口
- [x] 梳理现代化改造当前“已完成 / 进行中 / 待启动”总进度视图

### 3.3 P2：前端与体验层补充优化

- [x] 继续核对 `SkillsConfig.tsx`、`TemplatePanel.tsx`、`WebSearchPanel.tsx` 等高中文占比模块是否还存在零散口径不一致
- [x] 继续复查看板与 React 前端中仅用于兼容的内部 ID，确认不会再直接暴露到公开界面
- [ ] 在完成全仓重命名后补做一轮构建、联调与公开界面回归验证

## 四、后续建议执行顺序

为避免再次因上下文切换而扩散范围，后续建议严格按下面顺序推进。

| 顺序 | 建议动作 | 目标 |
|---|---|---|
| 1 | 做一轮前端构建与界面回归 | 确认收口未影响交付面 |
| 2 | 视构建结果决定是否还需补修公开界面文案 | 防止最后一轮回归后出现新残留 |
| 3 | 将本轮文档治理与归档整理写入工作日志 | 保持仓库留痕清晰 |
| 4 | 再决定是否提交新的收口 commit | 保持提交边界清楚 |
| 5 | 最后补同步 README / ROADMAP / Todo 的版本说明 | 固化最终状态 |

## 五、执行记录

- [x] 2026-04-08：已将仓库内多份 Todo 合并为当前统一主清单
- [x] 2026-04-08：已按实际仓库状态补打勾本轮已经完成但遗漏更新的事项
- [x] 2026-04-08：已将“README 预览图补齐、GitHub 推送、备份生成、全网搜索闭环、构建产物去纳管、最终留痕”等内容统一回写到主 Todo
- [x] 2026-04-08：已复核 P0 命名迁移主线，确认 `agents.json`、`agents/groups/`、`scripts/`、`tests/` 与主调用链已完成收口并回写勾选状态
- [x] 2026-04-08：已完成 README、ROADMAP、安装文档与长期治理入口的口径统一，并补出 `docs/current_capability_boundary.md`
- [x] 2026-04-08：已复核 `SkillsConfig.tsx`、`TemplatePanel.tsx`、`WebSearchPanel.tsx`，并移除技能详情弹窗中的内部路径展示
- [x] 2026-04-08：已将旧阶段性 Todo、审计检查单与 review 留痕集中归档到 `docs/archive/` 与 `review_notes/archive_20260408/`
- [ ] 后续新增任务必须继续追加到本文件，并在完成后立即更新勾选状态

## 六、旧文件处理原则

`docs/current_todo_20260408.md` 不再作为继续维护的主 Todo。后续应将其视为**上下文恢复阶段产生的临时待办记录**，保留仅用于追溯，不再继续增量更新。当前仓库唯一主 Todo 为根目录下的 `todo.md`。

本轮已进一步把旧阶段性文件集中归档，避免继续与主线文档混放：根目录旧清单已移入 `docs/archive/root_legacy_notes/`，阶段性恢复/审计文档已移入 `docs/archive/stage_notes_20260407_20260408/`，审计扫描与复核输出已移入 `review_notes/archive_20260408/`。
