# 运行看板浏览器核查记录（2026-04-07）

## 已确认事项

当前真实运行入口 `http://127.0.0.1:7891/` 已切换到手写 `dashboard/dashboard.html`，页面标题为“多Agent智作中枢”。

运行调度页中已经出现“直连调度中心”窗口，包含以下可见元素：

- 消息类型下拉框
- 关联任务 ID 输入框
- 调度消息文本框
- 四个快捷模板按钮
- 清空按钮
- “发送给调度中心”按钮
- Agent 在线状态区域

说明此前“新增直连调度中心窗口”的入口切换已实际生效。

## 浏览器中发现的问题

在运行调度页的 Agent 在线状态卡片里，仍然出现了旧体系标签，例如：

- 太子
- 中书省
- 门下省
- 尚书省
- 户部 / 礼部 / 兵部 / 刑部 / 工部 / 吏部

这说明虽然运行调度主卡片与大部分界面已改为现代中文体系，但 `agents-status` 返回字段或前端渲染链路仍有旧 label / role 直接透传到页面。

## 已开始处理的修正方向

已在 `dashboard/dashboard.html` 中补入统一的前端归一化能力，目标是：

1. 把旧部门名统一映射到现代中文显示标签。
2. 把 Agent 在线状态卡片中的 `label` / `role` / `statusLabel` 强制转换为现代中文显示。
3. 把实时活动流中的 `from` / `to` / `remark` 也统一经归一化后再展示。
4. 保留后端和历史数据兼容，不要求一次性清洗全部源数据。

## 后续验证重点

下一步应刷新浏览器并重点检查：

- Agent 在线状态卡片是否仍显示旧体系标签
- 运行调度页下半部分节点卡片是否存在代码专家 / 部署专家错配
- 调度中心直连发送后是否能联动触发状态刷新
- 增量刷新改造后倒计时与页面自动轮询是否仍正常工作

## 最新验证结果补记

依据最新浏览器截图，运行调度页的 Agent 在线状态区已经从旧体系标签切换到现代中文体系，当前可见卡片名称包括：

- 总控中心
- 规划中心
- 评审中心
- 调度中心
- 数据专家
- 文案专家
- 代码专家
- 合规专家
- 部署专家
- Agent管理专家

这说明前端新增的归一化显示逻辑已对真实运行页生效，至少在线状态区不再直接暴露“太子 / 中书省 / 六部”等旧名称。

下一步仍需继续检查的区域包括：

- 运行调度页下半区各中心与专家执行状态卡片
- 实时活动流与任务详情中的历史流转文案
- 模型配置、技能配置、结果归档、模板中心等其他标签页
- README 与 getting-started 等核心文档的残余旧术语

进一步下滚后的浏览器截图表明，运行调度页下半区“各中心与专家当前承接任务及执行状态”卡片同样已经完成现代中文体系切换。当前可见卡片标题包括“总控中心、规划中心、评审中心、调度中心、文案专家、数据专家、代码专家、合规专家、部署专家、Agent管理专家、情报简报”，且卡片内的状态摘要、实时性说明与空任务提示未再出现“太子、中书省、六部”等旧称呼。

这意味着真实运行入口页面中，运行调度页的上半区在线状态、直连调度中心窗口以及下半区执行状态卡片，已经基本完成用户可见层面的现代中文统一。后续优先级应转向其余标签页、任务详情历史流转、模型与技能配置页，以及 README、安装说明和规范文档中的残余旧术语与未兑现改造项。
